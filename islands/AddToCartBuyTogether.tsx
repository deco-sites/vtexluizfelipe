import type { Props } from "$store/components/ui/AddToCartButton.tsx";
import Component from "$store/components/ui/AddToCartButton.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
